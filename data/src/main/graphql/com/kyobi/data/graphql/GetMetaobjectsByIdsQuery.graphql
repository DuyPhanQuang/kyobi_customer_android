query GetMetaobjectsByIds($ids: [ID!]!) {
    nodes(ids: $ids) {
        ... on Metaobject {
            id
            handle
            type
            fields {
                key
                value
            }
        }
    }
}